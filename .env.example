# SQLite por defecto (desarrollo). Archivo: panaderia.db en la raíz del proyecto.
# DATABASE_URL=sqlite:///./panaderia.db

# PostgreSQL (producción)
# DATABASE_URL=postgresql+psycopg2://usuario:password@localhost:5432/panaderia
